apiVersion: 1

datasources:
  - name: MySQL
    type: mysql
    url: mysql:3306
    user: ${MYSQL_USER}
    secureJsonData:
      password: ${MYSQL_PASSWORD}
    database: ${MYSQL_DATABASE}
    isDefault: true
    jsonData:
      maxOpenConns: 10
      maxIdleConns: 5
      connMaxLifetime: 14400
